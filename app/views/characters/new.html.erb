<h1>New <%= @character.role.name %> <%= link_to die_image, "#roll", data: { roll: "everything" } %></h1>

<%= render partial: "form" %>
<p class="hint">
  Note that you can add gear and cyberware etc after creation is complete.
</p>
